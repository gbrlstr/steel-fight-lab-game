import type { Input } from '../shared/combat'
export function controls() { const keys = new Set<string>(); const codes = ['KeyA', 'KeyD', 'KeyW', 'KeyS', 'Space', 'AltLeft', 'ArrowLeft', 'ArrowRight', 'ArrowUp', 'ArrowDown', 'Enter', 'ShiftRight']; const down = (e: KeyboardEvent) => { if ((e.target as HTMLElement)?.matches('input,select,textarea')) return; if (codes.includes(e.code)) { e.preventDefault(); keys.add(e.code) } }; const up = (e: KeyboardEvent) => { keys.delete(e.code) }; const blur = () => keys.clear(); window.addEventListener('keydown', down); window.addEventListener('keyup', up); window.addEventListener('blur', blur); return { read(i: number): Input { const c = i ? ['ArrowLeft', 'ArrowRight', 'ArrowUp', 'ArrowDown', 'Enter', 'ShiftRight'] : ['KeyA', 'KeyD', 'KeyW', 'KeyS', 'Space', 'AltLeft']; const p = navigator.getGamepads?.()[i]; return { left: keys.has(c[0]) || !!p?.buttons[14]?.pressed || (p?.axes[0] ?? 0) < -.4, right: keys.has(c[1]) || !!p?.buttons[15]?.pressed || (p?.axes[0] ?? 0) > .4, up: keys.has(c[2]) || !!p?.buttons[12]?.pressed, down: keys.has(c[3]) || !!p?.buttons[13]?.pressed, attack: keys.has(c[4]) || !!p?.buttons[0]?.pressed, special: keys.has(c[5]) || !!p?.buttons[1]?.pressed } }, dispose() { window.removeEventListener('keydown', down); window.removeEventListener('keyup', up); window.removeEventListener('blur', blur) } } }
